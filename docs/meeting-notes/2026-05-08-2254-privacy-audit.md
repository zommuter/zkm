# 2026-05-08 — Privacy & leak audit for zkm on GitHub

**Started:** 2026-05-08 22:54
**Session:** 20a7906f-0756-42d9-8346-88fce7e2e805
**Attendees:** Archie (architect), Riku (devil's advocate), Petra (productivity)
**Topic:** Identify privacy issues and leaks in the `zommuter/zkm` GitHub repo; decide which to fix.

---

## Agenda

1. Catalogue scan findings
2. Rate severity — what actually needs fixing?
3. For fixable items: rewrite history vs. going-forward only?
4. Secondary scan: mail fixtures and test cases
5. Decide going-forward hygiene

---

## Discussion

### Item 1 — Scan findings

Full scan across all tracked files and git history:

| # | Issue | Where | Severity |
|---|-------|-------|----------|
| A | `claude.ai` chat URL in initial commit message | commit `5d8646c` | Medium |
| B | `@kienzler.dev` in Co-Authored-By on all Claude commits | git history (many commits) | Low |
| C | `$CLAUDE_SESSION_ID` literal (unexpanded) in some Co-Authored-By | git history (a few commits) | Cosmetic |
| D | Machine names (fievel, zomni, cartmanjaro) in meeting notes | docs/meeting-notes/*.md | Low |
| E | Local paths (~/src/, ~/mail/) in meeting notes | docs/meeting-notes/*.md | Low |

**Not found:** No credentials, API keys, passwords, or real personal email in tracked files. `.env` never committed.

---

### Item 2 — claude.ai URL in initial commit

**Archie:** Commit `5d8646c` has `Init from https://claude.ai/chat/04ec470c-…` in its message. That URL is now public on GitHub.

**Riku:** Three checks: (1) The conversation is architectural-only — CLAUDE.md and design docs, no credentials. (2) Anthropic auth gates access, so the URL alone doesn't grant entry. (3) Rewriting 100-commit history requires `git filter-repo`, then coordinated force-push to both `github` and `origin` (fievel). The rewrite cost is real; the threat is low.

**Petra:** Only one commit affected. N=2 for a full history rewrite fails on this item alone.

**Tobias decision:** Accept residual risk. No history rewrite.

---

### Item 3 — `@kienzler.dev` in Co-Authored-By

**Archie:** Deliberate pattern from git-diary-workflow skill. `kienzler.dev` is already publicly linkable to the zommuter account. Session UUIDs are UUIDs — no PII.

**Riku:** Not a privacy issue. The `$CLAUDE_SESSION_ID` literal commits (unexpanded variable) are a quality bug in the skill, not a security issue.

**Petra:** The skill fix belongs in `~/.claude/`, not in the zkm repo. Out of scope here.

**Tobias decision:** No action in zkm. Skill bug deferred to a separate session.

---

### Item 4 — Machine names and local paths in meeting notes

**Archie:** `docs/meeting-notes/*.md` contain fievel, zomni, cartmanjaro, `~/src/`, `~/mail/`.

**Riku:** Threat model is weak — machine names named after cartoon characters, standard local paths. Not actionable by an adversary.

**Tobias decision:** Leave as-is.

---

### Item 5 — Secondary scan: mail fixtures and test cases

**Archie:** `plugins/zkm-eml/` is a separate git repo (gitignored from main), only pushed to `fievel:src/zkm-plugins/zkm-eml.git`, not to GitHub. Its `.eml` fixtures use only `alice@example.com`, `bob@example.com` — fully synthetic. Main repo `tests/` has no real email addresses. Only ever-deleted tracked file is `zkm-init.sh` (clean shell script, no personal data). `hostname` appears in `zkm-init.sh` only as a shell expansion at install time, not committed.

**Riku:** Secondary scan confirms no additional findings.

**Petra:** Clean.

---

## Decisions

- **A (claude.ai URL in 5d8646c):** Accept residual risk. Conversation is architectural-only; Anthropic auth gates access. No history rewrite.
- **B (`@kienzler.dev` in Co-Authored-By):** Deliberate attribution pattern; domain already publicly linkable. No action.
- **C (`$CLAUDE_SESSION_ID` unexpanded):** Quality bug in `~/.claude` skill, not a zkm repo issue. Deferred to separate skill-fix session.
- **D/E (machine names / local paths in meeting notes):** Leave as-is. Low threat model for a personal project.
- **Mail/test data:** Confirmed clean. `zkm-eml` fixtures are synthetic and not on GitHub.

**Net verdict: no immediate remediation required in the zkm repo.**

---

## Action items

- [ ] (deferred, out of scope) Fix `$CLAUDE_SESSION_ID` expansion bug in git-diary-workflow or meeting skill — separate session in `~/.claude/`.
