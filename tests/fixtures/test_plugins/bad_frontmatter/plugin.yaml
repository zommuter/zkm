name: bad-frontmatter
version: 0.1.0
description: Plugin whose convert() emits frontmatter missing required fields.

creates_dirs:
  - notes

conformance:
  config:
    source_dir: tests/fixtures/corpus
