"""Plugin loader and converter dispatch. Phase 1 TODO."""

from pathlib import Path


def load_plugin(name: str, plugins_dir: Path) -> object:
    """Load a plugin by name from plugins_dir. Not yet implemented."""
    raise NotImplementedError("zkm convert — see TODO.md")
